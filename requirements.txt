# Pinned to the qiskit 1.x line.
#
# The local QAOA runs on a hand-written NumPy statevector solver (see
# python/execute.py), so we don't need qiskit-algorithms or qiskit-aer at all —
# qiskit-optimization just builds the QUBO. Dropping both keeps the serverless
# bundle well under Vercel's ~250 MB unzipped limit and speeds installs.
qiskit==1.4.2
qiskit-optimization==0.6.1
qiskit-ibm-runtime==0.29.0
numpy
scipy
